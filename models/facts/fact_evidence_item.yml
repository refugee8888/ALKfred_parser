version: 2

models:
  - name: fact_evidence_item
    columns:
     - name: eid
       tests:
          - not_null:
              config:
                tags: [critical]
          - unique:
              config:
                tags: [critical]

    