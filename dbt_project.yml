name: 'alkfred_dbt'
version: '1.0.0'
config-version: 2

# MUST match the profile name in your ~/.dbt/profiles.yml
profile: 'alkfred_parser'

model-paths: ['models']
analysis-paths: ['analysis']
test-paths: ['tests']
macro-paths: ['macros']
snapshot-paths: ['snapshots']

target-path: 'target'
log-path: 'logs'

models:
  alkfred_dbt:
    +materialized: view